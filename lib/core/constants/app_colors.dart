import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Emerald Theme
  static const Color primary = Color(0xFF059669);
  static const Color primaryDark = Color(0xFF064E3B);
  static const Color primaryLight = Color(0xFF34D399);

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444);

  static const Color menunggu = Color(0xFFFFC107); // kuning amber
  static const Color disetujui = Color(0xFF4CAF50); // hijau sukses
  static const Color ditolak = Color(0xFFF44336); // merah error
  static const Color selesai = Color(0xFF2196F3); // biru informasi
  static const Color lunas = Color(0xFF9C27B0);

  // Neutral Colors
  static const Color white = Colors.white;
  static const Color black = Color(0xFF111827);

  static const Color background = Color(0xFFF8FAFC);
  static const Color card = Color(0xFFFFFFFF);

  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);

  static const Color border = Color(0xFFD1D5DB);
  static const Color divider = Color(0xFFE5E7EB);

  // Attendance Status
  static const Color hadir = Color(0xFF22C55E);
  static const Color terlambat = Color(0xFFF59E0B);
  static const Color tidakHadir = Color(0xFFEF4444);
  static const Color izin = Color(0xFF3B82F6);
}
